# Dependencies are managed in pyproject.toml at the repo root.
# Install from the repo root with: pip install -e .

[project]
name = "paulana-zuplei-frontend"
version = "0.1.0"
description = "Paulana Zuplei - Sourcing Strategy Frontend"
requires-python = ">=3.12"
